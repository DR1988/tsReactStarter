import React from 'react'
import RootContainer from './'

describe('RootContainer container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
