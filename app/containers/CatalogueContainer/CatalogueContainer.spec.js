import React from 'react'
import CatalogueContainer from './'

describe('CatalogueContainer container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
