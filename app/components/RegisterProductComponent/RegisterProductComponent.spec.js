import React from 'react'
import RegisterProductComponent from './'

describe('RegisterProductComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
