import React from 'react'
import LoginComponent from './'

describe('LoginComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
