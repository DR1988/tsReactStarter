import React from 'react'
import CatalogItemComponent from './'

describe('CatalogItemComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
