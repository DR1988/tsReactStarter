import React from 'react'
import CatalogueComponent from './'

describe('CatalogueComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
