import React from 'react'
import RootComponent from './'

describe('RootComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
